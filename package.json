{
  "name": "workreport",
  "version": "1.0.0",
  "description": "Annotation Daily HQ - TailwindCSS build",
  "scripts": {
    "build:css": "tailwindcss -i ./input.css -o ./static/css/output.css --minify",
    "watch:css": "tailwindcss -i ./input.css -o ./static/css/output.css --watch",
    "tailwind:build": "tailwindcss -i ./input.css -o ./static/css/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}

